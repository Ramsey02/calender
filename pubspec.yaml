name: calender
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  
  # Original dependencies
  cupertino_icons: ^1.0.8
  
  # Essential dependencies for the calendar component
  flutter_animate: ^4.5.0
  font_awesome_flutter: ^10.7.0
  google_fonts: ^6.1.0
  provider: ^6.1.2
  intl: ^0.19.0
  
  # Additional dependencies you might need
  auto_size_text: ^3.0.0
  shared_preferences: ^2.3.2
  path_provider: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  # Add this if you have any assets like images
  # assets:
  #   - assets/images/